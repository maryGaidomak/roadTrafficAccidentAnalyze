export const COLLECTIONS = {
  roadSegments: 'road_segments',
  telemetryEvents: 'telemetry_events',
  incidentEvents: 'incident_events',
  riskAggregates: 'risk_aggregates',
  historicalAccidentStats: 'historical_accident_stats'
} as const;

export const DEFAULT_LIMIT = 20;
